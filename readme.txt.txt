This directory contains two files:
1. GUI.py
2. solver.py

To run the game simply run the GUI.py file from IDLE or the command line.

Note: You require pygame to play this game
install python and click path when there
go to settings -> system -> about -> advanced system settings -> enviornmental variables -> path -> edit
once there you can see if you have your 2 python paths that allow for pygame
should look like "C:\Users\<NAME>\AppData\Local\Programs\Python\Python311\Scripts\"
and also like    "C:\Users\<NAME>\AppData\Local\Programs\Python\Python311\" 
open command prompt and type "pip install pygame"
can check if worked when no error and when you open IDLE and type "import pygame", no errors